import { GameLayout } from "@/engine/GameLayout";

/** Binary Run — placeholder until gameplay is implemented. */
export function Game() {
  return (
    <GameLayout>
      <h1 className="game-title">Binary Run</h1>
      <p className="game-placeholder">Game Placeholder</p>
    </GameLayout>
  );
}
