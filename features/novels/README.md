# features/novels

Public books archive (`/books`) UI and registry that aggregates `features/stories/*`.

**Later migration from:** `app/books/page.tsx`, aggregator portion of `lib/content/books.ts`.

Does not own individual literary works — those live under `features/stories/`.
