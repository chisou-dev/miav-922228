# features/dashboard

Admin UI and site-control (feature flags / safety).

**Later migration from:** `app/admin/contacts/*` clients, `lib/site-control/*`.

**URLs stay:** `/admin/contacts`, `/api/admin/*`, `/api/site-control`.
