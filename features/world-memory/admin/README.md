# features/world-memory/admin

Admin-only World Memory operations (trace delete, story-memory seed handlers).

**Later migration from:** logic behind `app/api/admin/traces/*`, `app/api/admin/seed-story-memories`.

**URLs stay** under existing `app/api/admin/...` thin wrappers.
