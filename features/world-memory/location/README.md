# features/world-memory/location

Runtime catalog access: places + locations libraries.

**Later migration from:** `lib/places/*`, `lib/locations/*`.

**Stay at repo root:** `public/locations/**`, `data/locations/**` (stable URLs and build outputs).
