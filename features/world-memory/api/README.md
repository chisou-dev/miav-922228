# features/world-memory/api

HTTP handler modules for Trace Map APIs.

**Later migration from:** logic inside `app/api/trace`, `app/api/geo/ip`, `app/api/cron/cleanup-traces`.

**Route files stay** at the same `app/api/**/route.ts` paths (URL-stable).
