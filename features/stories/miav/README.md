# features/stories/miav

MIAV-922228 literary work: chapters, volume metadata, author page body.

**Later migration from:** `lib/content/chapters.ts`, `lib/chapters.ts`, MIAV entries in `lib/content/books.ts`, chapter/author page UI.

**Keep at repo root:** `content/chapters/**` (cwd-based loader).

**URLs stay:** `/chapters`, `/chapters/[slug]`, `/author`.
