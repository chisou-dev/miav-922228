# features/stories/_shared

Optional shared contracts for literary modules (types, helpers).

Empty in Phase 0. Add only when multiple stories need the same conventions — without changing runtime behavior prematurely.
