# features/contact

Contact form UI and contact domain helpers.

**Later migration from:** `app/contact/ContactForm.tsx`, `lib/contact/*`.

**URLs stay:** `/contact`, `/api/contact` via thin `app/` wrappers.
