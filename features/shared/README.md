# features/shared

Cross-cutting site chrome and utilities.

**Later migration from:** `components/SiteShell.tsx`, `lib/site.ts`, `lib/locale.ts`.

Used by most pages; keep imports stable after the move.
