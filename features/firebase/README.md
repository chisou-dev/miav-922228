# features/firebase

Shared Firebase client/admin/env/REST helpers.

**Later migration from:** `lib/firebase/*` (except ID-token verify → `features/auth`).

**Never move here:** `firebase.json`, `.firebaserc`, `firestore.rules`, `firestore.indexes.json`, `functions/`.
