# features/auth

Thin shared authentication primitives (e.g. Firebase ID token verification).

**Later migration from:** `lib/firebase/verifyFirebaseIdToken.ts`.

Feature-specific flows (Trace Google/anonymous, admin gate UI) stay with World Memory / Dashboard.
